// ⚠️  VENDORED — DO NOT EDIT.
// Copied verbatim from numa-lumi-branding-v2 src/shell/rig.ts @ cfc7b542a15e
// by scripts/sync-lumi3d.mjs. Change it upstream and re-run the script.

/** Default rig: an entity of radius 1 spans ~32% of the viewport height (hero size on a phone). */
export const DEFAULT_CAMERA = { position: [0, 0, 11] as [number, number, number], fov: 32 }
/** The entity sits a little above centre so the caption below never touches it. */
export const ENTITY_Y = 0.55
